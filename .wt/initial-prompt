You are working on task `s7-coordinator`: "S7 coordinator: state + verify + loopguard + tokenmonitor"

Task file: `tasks/s7-coordinator.md` (edit it to leave breadcrumbs for the next
session if you'll come back to this). When the unit lands and
`just check` + `just build` are green, run `wt merge` yourself -
that's the default next step on green, not a confirmation point.
(`wt abandon` if the work won't ship.) Either flips status=done.

As your absolute last action before exit, run
`wt task tell skyhelm "merged: s7-coordinator"` (or `"abandoned: s7-coordinator (<reason>)"`,
under 100 chars). Skyhelm needs that event to start the next queued task.

Ping skyhelm via `wt task tell skyhelm "<body>"` immediately on blockers:
merge conflict, red gate after a real fix attempt, ambiguous brief, scope
creep worth a new task. Mint follow-ups with
`wt task new --draft "<title>"` rather than tacking onto this one.

Before `wt task done` (or `pause` / `block`), drain `inbox/` (read each,
`mv` to `inbox/read/`). The flip refuses with exit 2 if anything is unread.

Do NOT open GitHub PRs (no `gh pr create`, no
`mcp__github__create_pull_request`). The landing path is `wt merge`:
a direct fast-forward into `main`. PRs are reserved for wingbot, which
can't push directly. If you think you need a PR, you don't - run
`wt merge` instead, or `wt task tell skyhelm "<reason>"` if it
actually won't fast-forward.

Brief:
**Sibling**: spore-s7-fleet-coordinator (in nix-config)

Build the coordinator packages in spore that skyhelm consumes.

## New packages

1. `coordinator/state` - state.md parser/writer (H2/H3 format, active tasks table, recent events, rules, directives)
2. `coordinator/verify` - port nix-config's `harness/skyhelm-verify-done.sh` verdict logic to Go. Verdicts: real-impl, rational-close, cross-repo, suspect-hallucination, bogus-evidence, unknown.
3. `coordinator/loopguard` - circuit breaker on respawn rate
4. `coordinator/tokenmonitor` - wraps budget short window into stop-hook shape
5. `coordinator/statedebt` - port skyhelm-state-debt scanner

Reference sources:
- `~/nix-config/harness/skyhelm-verify-done.sh`
- `~/nix-config/nix/packages/wt/skyhelm-state-debt`
- `~/nix-config/nix/packages/wt/skyhelm-token-monitor`

## New CLI

`cmd/spore coordinator {role-brief, state-debt, verify-done, loop-guard}`

## Evidence

- go-test: green for all 5 packages
- verify-verdicts: test cases cover all 6 verdict types

