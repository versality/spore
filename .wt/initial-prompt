You are working on task `s4-budget-parity`: "S4 budget: verify parity + active-tier subcommand"

Task file: `tasks/s4-budget-parity.md` (edit it to leave breadcrumbs for the next
session if you'll come back to this). When the unit lands and
`just check` + `just build` are green, run `wt merge` yourself -
that's the default next step on green, not a confirmation point.
(`wt abandon` if the work won't ship.) Either flips status=done.

As your absolute last action before exit, run
`wt task tell skyhelm "merged: s4-budget-parity"` (or `"abandoned: s4-budget-parity (<reason>)"`,
under 100 chars). Skyhelm needs that event to start the next queued task.

Ping skyhelm via `wt task tell skyhelm "<body>"` immediately on blockers:
merge conflict, red gate after a real fix attempt, ambiguous brief, scope
creep worth a new task. Mint follow-ups with
`wt task new --draft "<title>"` rather than tacking onto this one.

Before `wt task done` (or `pause` / `block`), drain `inbox/` (read each,
`mv` to `inbox/read/`). The flip refuses with exit 2 if anything is unread.

Do NOT open GitHub PRs (no `gh pr create`, no
`mcp__github__create_pull_request`). The landing path is `wt merge`:
a direct fast-forward into `main`. PRs are reserved for wingbot, which
can't push directly. If you think you need a PR, you don't - run
`wt merge` instead, or `wt task tell skyhelm "<reason>"` if it
actually won't fast-forward.

Brief:
**Sibling**: spore-s4-agent-budget (in nix-config)

Verify that `spore budget` has full parity with the standalone `agent-budget` binary in nix-config (`nix/packages/agent-budget/`).

## Check list

1. Subcommand parity: refresh, query, summary, stop-hook, debug-usage, active-tier, capture.
2. State-file format: byte-compatible state.json.
3. Env knobs: all AGENT_BUDGET_* vars recognized.
4. Stop-hook exit codes: exit 0 on ok, exit 2 + stderr on band crossing.
5. `active-tier` subcommand: must exist (used by skyhelm-spawn and rower-token-monitor).

## If gaps found

Implement the missing pieces. The `active-tier` subcommand reads the OAuth credentials and returns "max", "pro", "team", or "free". If it's missing from spore, port it from `nix/packages/agent-budget/agent-budget`.

Reference source: `~/nix-config/nix/packages/agent-budget/`

## Evidence

- go-test: green
- parity-check: `spore budget summary` matches `agent-budget summary` output format

